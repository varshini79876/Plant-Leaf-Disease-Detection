Place your trained model file here as:

    plant_disease_model.h5

To train the model, run from the project root:

    python train_model.py --dataset_path ./dataset

See README.md for full step-by-step instructions.

Without this file the app runs in Demo Mode — all pages work
but predictions are illustrative rather than real.
